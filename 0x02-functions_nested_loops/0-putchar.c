#include <main.h>


/**
 * main - Check description
 * Description: It prints the putchar, followed by a new line.
 * Return: 0.
 */
int main(void)
{
	int char;
       char i; 

	for (i = a; i <= z; i++)
		putchar(i);
	putchar('\n');

	return (0);
}
